System Monitor with Prometheus

This project monitors system resources (CPU, memory, disk) and exposes metrics for Prometheus.

Features
	•	CPU usage monitoring
	•	Memory usage monitoring
	•	Disk usage monitoring
	•	Prometheus metrics endpoint

Usage

pip install -r requirements.txt
python app.py

Metrics available at:
http://localhost:8000
